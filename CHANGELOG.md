# Changelog

## v0.2.0 — 2025-05

### 浮动工具栏（SelectionToolbar）全面增强

- **新增「复制」按钮**：选中文字后，工具栏可直接复制选中内容到剪贴板。
- **搜索引擎图标本地优先**：弹出工具栏时永远只使用本地已下载的图标（`assets/icons/`），不再实时在线访问，极大提升显示速度。
- **Favicon 内存缓存**：实现了图标内存缓存机制，避免重复读取存储。
- **图标自动下载**：
  - 内置搜索引擎图标通过 `node scripts/download-engine-icons.mjs` 批量下载到 `assets/icons/`。
  - 用户自定义搜索引擎后，图标自动下载到本地文件夹；下载失败时提示用户手动设置图标网址。
  - 每个搜索引擎设置项中增加「下载图标」按钮，可手动触发下载。
- **图标来源优化**：图标下载地址直接从引擎提供的 URL 域名发现（`https://{domain}/favicon.ico` 等），不再依赖 DuckDuckGo 等第三方服务。
- **文字图标替代**：除图片图标外，每个搜索引擎允许用一个汉字或 Emoji（如 🧬、汉、维）作为图标显示。
- **允许修改已设置的搜索引擎**：设置面板中支持编辑已有引擎的名称、URL 和图标。
- **新增搜索引擎**：
  - **江南知识库** `https://q.natanalysis.com/search?q=%s`
  - **PubMed** `https://pubmed.ncbi.nlm.nih.gov/?term=%s`
- **引擎配置统一化**：`src/services/storage.ts` 的 `DEFAULT_ENGINES` 直接读取 `src/services/engines.json`，不再重复维护两份列表；本地图标映射统一放在 `src/services/color.ts`。
- **性能优化**：优化工具栏渲染逻辑，标注按钮与搜索引擎按钮分离加载，减少弹出延迟；加快选中文字后的响应速度。

### 批注与标注

- **单个批注颜色设置**：每个批注可独立设置颜色（覆盖全局高亮颜色），支持预设色和自定义色。
- **暗色模式适配**：检测系统 `prefers-color-scheme: dark`，高亮颜色在暗色模式下自动加深（如 `rgba(250,204,21,0.3)` → `rgba(250,204,21,0.15)`），阅读更舒适。

### 侧边栏 UI 优化

- **头部区域更紧凑**：标题字号缩小、按钮统一为 `28×28` 圆角、移除收藏时间独立显示。
- **标签区域智能显示**：无标签时自动隐藏整行，节省垂直空间。
- **颜色选择器精简**：移除文字标签，色块缩小为 `14px`。
- **搜索与添加区域整合**：搜索框新增搜索图标与一键清除按钮；与添加笔记按钮合并到同一区域。
- **列表区域优化**：排序/搜索状态栏改为 sticky 吸顶；列表内边距与卡片间距缩小。
- **Footer 大幅简化**：语言切换改为分段式 toggle 按钮；导入/导出从 4 个平铺按钮合并为 2 个下拉菜单组。

### 数据导入导出

- **格式改为 Markdown**：数据导出/导入从 JSON 格式改为 Markdown 格式，更便于人工阅读和版本控制。

### 代码质量

- **清理调试语句**：移除代码中散落的 `console.log` 调试输出。

---

## v0.1.0 — 2025-04

- 初始版本发布。
- 支持文本高亮、下划线、删除线、波浪线四种标注样式。
- 支持 Chrome 原生 Side Panel、上下文菜单、便签、层级回复。
- 支持多引擎搜索浮动工具栏、书签文件夹管理、全局搜索。
